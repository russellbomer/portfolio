// The pinwheel is disabled on /datum (see GlobalDecor's NO_PINWHEEL_ROUTES),
// so content stays simply centered rather than shifted left to clear it.
// The fixed ThemeToggle/BackToTop circles are small enough not to need
// dedicated clearance.
// Anchored to the "What I do" section's width so every section shares one
// consistent content column.
export const CONTAINER = "mx-auto max-w-4xl px-4 sm:px-6";
